Virtual DOM
คือ DOM เสมือน ที่คัดลอก DOM จริงๆมาเก็บลง memory แล้วถ้ามีการเปลี่ยนแปลงเกิดขึ้นที่ component ก็จะอัพเฉพาะ component ที่เปลี่ยนแปลงเท่านั้น 
โดยไม่จำเป็นต้องอัพ DOM ใหม่ทั้งหมด (เพราะDOMปกติจะRefreshทั้งหน้าเพื่ออัพหน้าเว็บ) ทำให้ทำงานได้อย่างรวดเร็ว 